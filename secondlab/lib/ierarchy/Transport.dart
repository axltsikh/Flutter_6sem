class Transport{
  int id;
  int capacity;
  double weight;
  int year;
  String brand;
  String model;
  Transport({required this.id,required this.capacity,required this.weight,required this.year,required this.brand,required this.model});
}